<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hnguigu.spring.mapper.DepartmentMapper">

    <insert id="save">
        INSERT INTO SPRING_DEPARTMENT(DEPARTMENT_ID, DEPARTMENT_NAME, DEPARTMENT_LOCATION)
        VALUES (#{id}, #{name}, #{location})
    </insert>
</mapper>